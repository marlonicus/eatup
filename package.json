{
  "name": "eatup",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "LAMBDA_HOST=http://localhost:9000 concurrently \"netlify-lambda serve functions\" \"node build/envs.js && next\"",
    "build": "node build/envs.js && netlify-lambda build functions && next build && next export"
  },
  "dependencies": {
    "next": "^6.0.3",
    "ramda": "^0.25.0",
    "react": "^16.4.1",
    "react-dom": "^16.4.1"
  },
  "devDependencies": {
    "netlify-lambda": "^0.4.0"
  }
}
